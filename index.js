/* eslint-disable max-len */
const partition = require('./partition-problem');

const result = partition.allocateBallsIntoUrns([1, 1], [1, 1]);
// const result = partition.allocateBallsIntoUrns([2, 2, 2, 2], [1, 1, 1, 1, 1, 1, 1, 1]);
console.log(result);
